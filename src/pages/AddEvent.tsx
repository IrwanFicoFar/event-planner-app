import { FC } from "react";
import { Layout } from "../components/Layout";

const AddEvent: FC = () => {
  return (
    <Layout>
      <h1> Add Event Page</h1>
    </Layout>
  );
};

export default AddEvent;

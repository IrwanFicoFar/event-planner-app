import { FC } from "react";
import { Layout } from "../components/Layout";

const Profile: FC = () => {
  return (
    <Layout>
      <h1> Profile Page</h1>
    </Layout>
  );
};

export default Profile;
